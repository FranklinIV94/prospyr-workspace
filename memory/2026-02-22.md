# 2026-02-22

## Context
- Goal: fix automation + deploy Vercel intake app correctly (https://dist-brown-seven-57.vercel.app/).
- Browser Relay/Chrome extension is pointed at a different OpenClaw gateway, so the relay/tab attach workflow cannot be used from this session.
- Local OpenClaw `browser` tool failed with: Chrome extension relay running but **no tab connected** (needs attached tab on profile="chrome").

## Findings (from live deploy)
- Deployed page loads and serves JS bundle.
- EmailJS is bundled client-side with these identifiers:
  - service_id: `service_3xdfe0k`
  - template_id: `template_3z8uxeq`
  - public key: `UpJpa9FcJ9pfZKN6R`
- EmailJS send is attempted in the frontend; likely failure causes include:
  - EmailJS dashboard missing **Allowed origins/domains** for `https://dist-brown-seven-57.vercel.app` (and production domain)
  - template param mismatch / blocked key / CORS

## What we need next (when Prospyr gateway is the active gate)
1. Attach the Vercel tab to the correct OpenClaw gateway via Browser Relay.
2. Reproduce a submission and read **Console + Network** to capture the exact runtime error.
3. Apply fix (most likely: add Allowed Origins in EmailJS), redeploy, verify.

## Notes
- Direct API test to EmailJS from server returned 403: "API calls are disabled for non-browser applications" (expected; browser-only).
