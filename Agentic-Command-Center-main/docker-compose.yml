version: '3.8'

services:
  command-center:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./command_center.db:/app/command_center.db
    restart: always
